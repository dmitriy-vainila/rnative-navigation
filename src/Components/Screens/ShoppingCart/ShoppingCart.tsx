import React from "react";
import { Text, View } from "react-native";

import { StyledMainView, StyledText } from "../Profile/styled";

const ShoppingCart = () => {
   return (
      <StyledMainView>
         <StyledText>Shopping Cart</StyledText>
      </StyledMainView>
   );
};

export default ShoppingCart;
